# Пісочниця 02 — Біти не брешуть: шість маленьких задач

**Тижні:** 3–4 · **Лаба:** [Lab 02](lab-02-bits-dont-lie.md) · **Notes:** [досліди](lab-02-bits-dont-lie.notes.md) · **Курс:** [EN](README.md) · [UK](README.uk.md)

> **Що це.** Пісочниця — одна з двох стежок курсу, простіший варіант. На основній ви
> лабу за лабою будуєте `ember`. Тут проєкту немає: замість нього шість незалежних
> задач по 30–50 хвилин, і кожна розкриває одну ідею з теорії Lab 2. Задачі між
> собою не пов'язані: не вийшла одна — беріть наступну.
>
> Теорія та сама, що й у [Lab 02](lab-02-bits-dont-lie.md): прочитайте розділ «Теорія»
> там. Кожна задача має дві половини — **Передбачте** і **Напишіть самі**; як із ними
> працювати, описано в [Пісочниці 01](lab-01-a-box-of-bytes.sandbox.md#як-працювати).
> Прогнози пишіть у `sandbox-02.md`, власний код — у теку `sandbox-02/`.

---

## 1. Парне чи непарне

Процесор не ділить, щоб перевірити парність: йому досить подивитись на один молодший біт.

### Передбачте

```cpp
#include <cstdint>
#include <iostream>
int main() {
    int numbers[] = {7, 10, -3};
    for (int i = 0; i < 3; ++i) {
        int x = numbers[i];
        std::cout << x << ": " << (x % 2) << ' ' << (x & 1) << '\n';
    }
}
```

**Вгадайте:** що надрукують `x % 2` і `x & 1` для кожного з трьох чисел?

**Зміни одне:** котрий із двох виразів можна брати, щоб перевірити «непарне», якщо числа бувають від'ємні: `x % 2 == 1` чи `(x & 1) == 1`? Відповідь уже є у виводі.

### Напишіть самі

Коли байт летить по UART, до нього часто додають **біт парності**: одиницю, якщо в байті непарна кількість одиниць. Так приймач помічає, що один біт зіпсувався. Напишіть `bool parity(std::uint8_t b)`, яка рахує одиниці через `& 1` і `>>` у циклі.

Для `parity(0xA3)`, `parity(0x07)` і `parity(0xFF)` має надрукуватись: `0 1 0`

**Що це означає:** молодший біт каже про парність і для від'ємних чисел, а `%` зберігає знак ([теорія §3](lab-02-bits-dont-lie.md#3-шість-операторів-і-два-з-них-кусаються)).

---

## 2. Степінь двійки одним рядком

У степеня двійки рівно одна одиниця в двійковому записі. Вираз `x & (x - 1)` знімає наймолодшу одиницю — отже, для степеня двійки дає нуль.

### Передбачте

```cpp
#include <cstdint>
#include <iostream>
int main() {
    int numbers[] = {1, 2, 6, 64, 96, 0};
    for (int i = 0; i < 6; ++i) {
        int x = numbers[i];
        bool power = (x & (x - 1)) == 0;
        std::cout << x << (power ? " так" : " ні") << '\n';
    }
}
```

**Вгадайте:** для кожного з шести чисел — «так» чи «ні»?

**Зміни одне:** рядок `bool power = …` замініть на `x != 0 && (x & (x - 1)) == 0`. Що змінилось?

### Напишіть самі

Напишіть `int next_pow2(int x)`: найменший степінь двійки, не менший за `x`. Почніть з 1 і зсувайте ліворуч, поки не дорівняєтесь.

Для `next_pow2(5)`, `next_pow2(64)` і `next_pow2(100)` має надрукуватись: `8 64 128`

**Що це означає:** `x - 1` перевертає всі біти від наймолодшої одиниці вниз, а `&` лишає лише те, що вище ([теорія §4](lab-02-bits-dont-lie.md#4-маски-як-інструкція-розкладається-на-поля)). Нуль — пастка: одиниць у ньому немає зовсім.

---

## 3. Геймпад в одному байті

Контролер приставки NES передавав стан усіх восьми кнопок одним байтом: кожна кнопка — свій біт.

### Передбачте

```cpp
#include <cstdint>
#include <iostream>
const std::uint8_t UP = 1u << 0, DOWN = 1u << 1, LEFT = 1u << 2, RIGHT = 1u << 3;
const std::uint8_t BTN_A = 1u << 4, BTN_B = 1u << 5;
int main() {
    std::uint8_t pad = 0;
    pad |= UP;
    pad |= BTN_A;
    pad |= RIGHT;
    pad &= (std::uint8_t)~UP;
    std::cout << (int)pad << ":";
    std::uint8_t buttons[] = {UP, DOWN, LEFT, RIGHT, BTN_A, BTN_B};
    for (int i = 0; i < 6; ++i) {
        std::cout << ' ' << ((pad & buttons[i]) != 0);
    }
    std::cout << '\n';
}
```

**Вгадайте:** число після `pad` і шість нулів чи одиниць: натиснуті кнопки в порядку UP, DOWN, LEFT, RIGHT, A, B.

**Зміни одне:** після рядка з `&= ~UP` допишіть `pad ^= LEFT;` двічі, а потім `pad ^= DOWN;`. Що тепер?

### Напишіть самі

Напишіть `void print_pad(std::uint8_t pad)`, яка друкує назви натиснутих кнопок через пробіл, у порядку UP, DOWN, LEFT, RIGHT, A, B. Константи візьміть з коду вище.

Для `print_pad(0x19)` і `print_pad(0x22)` має надрукуватись:

```txt
UP RIGHT A
DOWN B
```

**Що це означає:** `|=` виставляє біт, `&= ~` скидає, `^=` перемикає, `&` перевіряє ([теорія §4](lab-02-bits-dont-lie.md#4-маски-як-інструкція-розкладається-на-поля)).

---

## 4. Пастка пріоритету

Один із двох багів, на які в цій лабі наступає кожен.

### Передбачте

```cpp
#include <cstdint>
#include <iostream>
int main() {
    int flags = 6;
    if (flags & 4 == 4) {
        std::cout << "bit 2 is set\n";
    } else {
        std::cout << "bit 2 is clear\n";
    }
}
```

**Вгадайте:** що надрукує програма: `set` чи `clear`? Потім зберіть.

**Зміни одне:** спершу приберіть `-Werror` з рядка збірки й запустіть. Потім поверніть його й візьміть умову в дужки: `(flags & 4) == 4`.

### Напишіть самі

Напишіть `bool bit_is_set(int value, int n)` — з дужками там, де треба.

Для `value = 6` і `n = 0, 1, 2` має надрукуватись: `0 1 1`

**Що це означає:** `==` зв'язує тісніше за `&`, тож без дужок це `flags & (4 == 4)` ([теорія §3](lab-02-bits-dont-lie.md#3-шість-операторів-і-два-з-них-кусаються)). `-Werror` рятує вас від цього бага.

---

## 5. Зсув замість множення

Зсув ліворуч на n — це множення на 2ⁿ. Але біти, що виїхали за край, зникають.

### Передбачте

```cpp
#include <cstdint>
#include <iostream>
int main() {
    int x = 5;
    std::cout << (x << 3) << '\n';
    std::uint8_t b = 200;
    b = (std::uint8_t)(b << 1);
    std::cout << (int)b << '\n';
    int neg = -16;
    std::cout << (neg >> 2) << '\n';
}
```

**Вгадайте:** три числа.

**Зміни одне:** в останньому рядку замініть `(neg >> 2)` на `((std::uint8_t)neg >> 2)`.

### Напишіть самі

У маленьких контролерах множення буває повільним. Напишіть `int times10(int x)`, яка множить на 10 лише зсувами й додаванням. Підказка: 10 = 8 + 2.

Для `times10(7)` і `times10(25)` має надрукуватись: `70 250`

**Що це означає:** зсув рухає біти; у знакового числа праворуч зверху заходять копії знакового біта ([теорія §3](lab-02-bits-dont-lie.md#3-шість-операторів-і-два-з-них-кусаються)).

---

## 6. Колір у трьох байтах

Колір `#FF8800` — це три байти: червоний, зелений, синій. Маленькі кольорові дисплеї в пристроях часто беруть лише 16 бітів на колір — формат RGB565.

### Передбачте

```cpp
#include <cstdint>
#include <iostream>
int main() {
    std::uint32_t orange = 0xFF8800;
    int r = (orange >> 16) & 0xFF;
    int g = (orange >> 8) & 0xFF;
    int b = orange & 0xFF;
    std::cout << r << ' ' << g << ' ' << b << '\n';
}
```

**Вгадайте:** три числа.

**Зміни одне:** після рядка з друком додайте упаковку в RGB565: `std::uint16_t rgb565 = (std::uint16_t)(((r >> 3) << 11) | ((g >> 2) << 5) | (b >> 3));` і надрукуйте її через `std::hex`.

### Напишіть самі

Тепер у зворотний бік. Напишіть `std::uint32_t pack(int r, int g, int b)`, яка складає три байти в одне число `0xRRGGBB`, і надрукуйте результат через `std::hex`.

Для `pack(255, 136, 0)` і `pack(0, 128, 255)` має надрукуватись: `ff8800 80ff`

**Що це означає:** поле дістають зсувом до нуля й маскою, а пакують зсувом на місце й `|` ([теорія §4](lab-02-bits-dont-lie.md#4-маски-як-інструкція-розкладається-на-поля)). Де такі дисплеї — [у залізі](HARDWARE.md#lab-2--біти-не-брешуть).

---

## Здача

У репозиторії:

- файл `sandbox-02.md`, де для кожної задачі є таблиця «мій прогноз · справжній
  вивід · чому так (одне речення)»;
- тека `sandbox-02/` з вашим кодом до «Напишіть самі»: `task1.cpp`, `task2.cpp` і так далі;
- тег `sandbox-02`.

Рівні ті самі, що й в основній лабі, і важать так само:

- **Basic — зараховано:** чотири задачі з шести, обидві половини кожної.
- **Standard — цільовий:** усі шість задач, обидві половини.
- **Advanced — відмінно:** усі шість задач разом із «Зміни одне» в кожній.

**На захисті** викладач бере одну з ваших задач і просить дві речі: передбачити
вивід готового коду після того, як він змінить у ньому одне число, і пояснити або
змінити ваш власний код до «Напишіть самі». Потім одне питання з
[розділу «На захисті» лаби](lab-02-bits-dont-lie.md#на-захисті--поясніть-біля-дошки).

---

<details>
<summary>Для викладача: відповіді (перевірено запуском на clang і gcc)</summary>

| Задача | «Передбачте» | «Зміни одне» |
|---|---|---|
| 1 | `7: 1 1`, `10: 0 0`, `-3: -1 1` | лише `(x & 1) == 1`: для −3 `x % 2` дорівнює −1 |
| 2 | `1 так`, `2 так`, `6 ні`, `64 так`, `96 ні`, `0 так` — нуль помилково вийшов «степенем» | тепер `0 ні`, решта без змін |
| 3 | `24: 0 0 0 1 1 0` | `26: 0 1 0 1 1 0`: два перемикання LEFT скасували одне одне |
| 4 | не збереться: і clang, і gcc попереджають про пріоритет, а `-Werror` робить з цього помилку | без `-Werror` — `bit 2 is clear`, бо `6 & 1` дорівнює 0; з дужками — `bit 2 is set` |
| 5 | `40`, `144` (400 не влізло в байт), `-4` | `60`: −16 як байт — це 240, а 240 / 4 = 60 |
| 6 | `255 136 0` | друге число — `fc40` |

Очікуваний вивід «Напишіть самі» студент бачить у задачі. Розв'язки до кожної з них
написано й запущено, вивід саме такий. На захисті найкраще працює прохання змінити
одне число у власному коді студента й сказати, що тепер надрукується.

</details>
