# Пісочниця 03 — Адреси, а не імена: сім маленьких експериментів
**Тижні:** 5–6 · **Лаба:** [Lab 03](lab-03-addresses-not-names.md) · **Notes:** [досліди](lab-03-addresses-not-names.notes.md) · **Курс:** [EN](README.md) · [UK](README.uk.md)
> **Що це.** Пісочниця — одна з двох стежок курсу. На основній ви лабу за лабою
> будуєте `ember`. Тут проєкту немає: замість нього сім незалежних задач по 20–40
> хвилин, і кожна розкриває одну ідею з теорії Lab 3. Задачі між собою не пов'язані:
> не вийшла одна — беріть наступну.
>
> Теорія та сама, що й у [Lab 03](lab-03-addresses-not-names.md): прочитайте розділ «Теорія»
> там. Тут лише задачі. Як працювати і як здавати — так само, як у
> [Пісочниці 01](lab-01-a-box-of-bytes.sandbox.md#як-працювати): спершу прогноз у
> `sandbox-03.md`, потім запуск.

---
## 1. Відстань між сусідами

Сусідні елементи масиву лежать у пам'яті впритул. Але «відстань» між ними залежить від того, чим міряти.

```cpp
#include <cstdint>
#include <iostream>
int main() {
    int a[3] = {10, 20, 30};
    std::cout << (&a[1] - &a[0]) << '\n';
    std::cout << ((char*)&a[1] - (char*)&a[0]) << '\n';
    std::cout << (&a[2] - &a[0]) << '\n';
}
```

**Вгадайте:** три числа.

**Зміни одне:** замініть `int a[3]` на `double a[3]`.

**Що це означає:** різниця вказівників рахує елементи, а різниця `char*` — байти ([теорія §2](lab-03-addresses-not-names.md#2-у-вказівника-є-тип-щоб--і--знали-розмір)).

---
## 2. Обмін, який не працює

Класика: функція, яка мала поміняти змінні місцями, а нічого не змінила.

```cpp
#include <cstdint>
#include <iostream>
void swap_copy(int x, int y) {
    int t = x;
    x = y;
    y = t;
}
void swap_ptr(int* x, int* y) {
    int t = *x;
    *x = *y;
    *y = t;
}
int main() {
    int a = 1, b = 2;
    swap_copy(a, b);
    std::cout << a << ' ' << b << '\n';
    swap_ptr(&a, &b);
    std::cout << a << ' ' << b << '\n';
}
```

**Вгадайте:** що надрукують обидва рядки.

**Зміни одне:** після `swap_ptr(&a, &b);` допишіть `swap_ptr(&a, &a);`. Що буде?

**Що це означає:** копія живе у своєму кадрі, а вказівник веде до оригіналу ([теорія §1](lab-03-addresses-not-names.md#1-імена-живуть-у-компіляторі-адреси--у-машині)).

---
## 3. Крокуємо вказівником

Цикл по масиву можна написати без індексу — лише вказівником, який іде вперед.

```cpp
#include <cstdint>
#include <iostream>
int main() {
    int a[5] = {3, 1, 4, 1, 5};
    int* p = a;
    int sum = 0;
    for (int i = 0; i < 5; ++i) {
        sum = sum + *p;
        p = p + 1;
    }
    std::cout << sum << '\n';
    std::cout << (p - a) << '\n';
}
```

**Вгадайте:** суму і те, на скільки елементів пішов `p`.

**Зміни одне:** почніть з `int* p = a + 1;` і пройдіть лише 4 кроки (`i < 4`).

**Що це означає:** `p + 1` — наступний елемент, а `p - a` — скільки елементів пройдено ([теорія §2](lab-03-addresses-not-names.md#2-у-вказівника-є-тип-щоб--і--знали-розмір)).

---
## 4. Скільки важить адреса

Адреса — теж число, і його розмір не залежить від того, на що вона вказує.

```cpp
#include <cstdint>
#include <iostream>
int main() {
    int i = 0;
    double d = 0;
    char c = 0;
    int* pi = &i;
    double* pd = &d;
    char* pc = &c;
    std::cout << sizeof(pi) << ' ' << sizeof(*pi) << '\n';
    std::cout << sizeof(pd) << ' ' << sizeof(*pd) << '\n';
    std::cout << sizeof(pc) << ' ' << sizeof(*pc) << '\n';
}
```

**Вгадайте:** три пари чисел.

**Зміни одне:** подумайте без запуску: що надрукує `sizeof(pc) + sizeof(*pc)`?

**Що це означає:** `sizeof(p)` — ширина адреси, `sizeof(*p)` — розмір того, на що вона вказує ([теорія §4](lab-03-addresses-not-names.md#4-sizeof-вирівнювання-і-чому-правду-каже-дамп)).

---
## 5. Порядок байтів вашого ноутбука

Протокол MAVLink, яким спілкуються дрони, записує числа молодшим байтом уперед, а мережеві протоколи — навпаки. Яким уперед пише ваш ноутбук?

```cpp
#include <cstdint>
#include <cstring>
#include <iostream>
int main() {
    std::uint32_t value = 0x12345678;
    unsigned char bytes[4];
    std::memcpy(bytes, &value, 4);
    std::cout << std::hex;
    for (int i = 0; i < 4; ++i) {
        std::cout << (int)bytes[i] << ' ';
    }
    std::cout << '\n';
}
```

**Вгадайте:** чотири байти в тому порядку, у якому вони лежать у пам'яті.

**Зміни одне:** замініть число на `0xFF`.

**Що це означає:** чесний спосіб подивитись на байти — `std::memcpy` ([теорія §3](lab-03-addresses-not-names.md#3-void--це-адреса-без-типу)); ваш ноутбук кладе молодший байт першим.

---
## 6. Як виглядає 1.0f у бітах

Число з рухомою комою — це знак, порядок і мантиса в одних 32 бітах. Датчики часто передають саме їх.

```cpp
#include <cstdint>
#include <cstring>
#include <iostream>
int main() {
    float f = 1.0f;
    std::uint32_t bits = 0;
    std::memcpy(&bits, &f, sizeof(bits));
    std::cout << std::hex << bits << '\n';
    f = -1.0f;
    std::memcpy(&bits, &f, sizeof(bits));
    std::cout << bits << '\n';
}
```

**Вгадайте:** чим відрізнятимуться два числа.

**Зміни одне:** замість `-1.0f` візьміть `2.0f`.

**Що це означає:** `memcpy` у `uint32_t` — визначений спосіб побачити біти `float`, а приведення `float*` до `int*` — ні ([теорія §3](lab-03-addresses-not-names.md#3-void--це-адреса-без-типу)).

---
## 7. Один крок за край

Вказівник на останній елемент — законно. Ще один крок — і ви читаєте чужу пам'ять.

```cpp
#include <cstdint>
#include <iostream>
int main() {
    int a[3] = {1, 2, 3};
    int* p = a + 2;
    std::cout << *p << '\n';
    p = p + 1;
    std::cout << *p << '\n';
}
```

**Вгадайте:** що надрукує програма і хто зупинить другий рядок.

**Зміни одне:** замініть перші три рядки `main` на `int* p = nullptr;` і `(void)a;`.

**Що це означає:** санітайзер ловить вихід за межу там, де «ну, надрукувало щось» мовчить ([теорія §1](lab-03-addresses-not-names.md#1-імена-живуть-у-компіляторі-адреси--у-машині)).

---
## Здача

У репозиторії — файл `sandbox-03.md` з таблицею «задача · мій прогноз · справжній
вивід · чому так (одне речення)» і тег `sandbox-03`.

- **Basic — зараховано:** п'ять задач із семи, з прогнозом, виводом і поясненням.
- **Standard — цільовий:** усі сім задач разом із «Зміни одне».
- **Advanced — відмінно:** усе вище плюс **власна восьма задача** в тому самому
  форматі на ідею з теорії Lab 3, якої не торкнулась жодна з семи — скажімо, `void*`, вказівник на вказівник чи різницю між `const int*` і `int* const`.
  На захисті її пояснюєте ви.

**На захисті** викладач бере одну з ваших задач, змінює в ній одне число й просить
передбачити вивід **до** запуску. Потім одне питання з
[розділу «На захисті» лаби](lab-03-addresses-not-names.md#на-захисті--поясніть-біля-дошки).

---

<details>
<summary>Для викладача: відповіді (перевірено запуском на clang і gcc)</summary>

| Задача | Відповідь | «Зміни одне» |
|---|---|---|
| 1 | `1`, `4`, `2` | `1`, `8`, `2` |
| 2 | `1 2`, потім `2 1` | нічого не зміниться: обмін змінної самої з собою |
| 3 | `14`, `5` | `11`, `5` |
| 4 | `8 4`, `8 8`, `8 1` на 64-бітній машині | `9` |
| 5 | `78 56 34 12` | `ff 0 0 0` |
| 6 | `3f800000`, `bf800000`: відрізняється лише старший біт — знак | другий рядок — `40000000` |
| 7 | `3`, а потім звіт санітайзера: на clang — ASan `stack-buffer-overflow`, на gcc — спершу UBSan `insufficient space for an object`, далі ASan | UBSan `load of null pointer`, потім аварійна зупинка |

</details>
