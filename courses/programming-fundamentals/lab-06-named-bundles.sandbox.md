# Пісочниця 06 — Іменовані набори: шість маленьких задач

**Тижні:** 11–12 · **Лаба:** [Lab 06](lab-06-named-bundles.md) · **Notes:** [досліди](lab-06-named-bundles.notes.md) · **Курс:** [EN](README.md) · [UK](README.uk.md)

> **Що це.** Пісочниця — одна з двох стежок курсу, простіший варіант. На основній ви
> лабу за лабою будуєте `ember`. Тут проєкту немає: замість нього шість незалежних
> задач по 30–50 хвилин, і кожна розкриває одну ідею з теорії Lab 6. Задачі між
> собою не пов'язані: не вийшла одна — беріть наступну.
>
> Теорія та сама, що й у [Lab 06](lab-06-named-bundles.md): прочитайте розділ «Теорія»
> там. Кожна задача має дві половини — **Передбачте** і **Напишіть самі**; як із ними
> працювати, описано в [Пісочниці 01](lab-01-a-box-of-bytes.sandbox.md#як-працювати).
> Прогнози пишіть у `sandbox-06.md`, власний код — у теку `sandbox-06/`.

---

## 1. Скільки коштує порядок полів

Ті самі три поля, лише в іншому порядку, займають різну кількість пам'яті.

### Передбачте

```cpp
#include <cstdint>
#include <iostream>
struct Wasteful {
    char a;
    int b;
    char c;
};
struct Tidy {
    int b;
    char a;
    char c;
};
int main() {
    std::cout << sizeof(Wasteful) << ' ' << sizeof(Tidy) << '\n';
}
```

**Вгадайте:** два числа.

**Зміни одне:** додайте в `Tidy` ще два поля `char d; char e;`. Потім натомість замініть у `Tidy` `int b` на `double b`.

### Напишіть самі

Ось пакет телеметрії: `struct Packet { char type; double value; char flags; int id; };`. Переставте поля так, щоб він займав якомога менше, і надрукуйте `sizeof` обох варіантів.

Має надрукуватись: `24 16`

**Що це означає:** компілятор вирівнює поля й додає вирівнювальні байти ([теорія §1](lab-06-named-bundles.md#1-struct--це-поля-поруч-у-яких-є-імена)).

---

## 2. Звичайний enum і enum class

Звичайний `enum` тихо перетворюється на число. `enum class` — ні.

### Передбачте

```cpp
#include <cstdint>
#include <iostream>
enum Color { Red, Green, Blue };
int main() {
    Color c = Green;
    int x = c + 10;
    std::cout << x << '\n';
}
```

**Вгадайте:** одне число.

**Зміни одне:** замініть `enum` на `enum class`. Виправте, що скаже компілятор, і зберіть ще раз.

### Напишіть самі

Напишіть `enum class Dir { North, East, South, West };` і функцію `Dir turn_right(Dir d)` через `switch`. Почніть з `North`, поверніть праворуч п'ять разів і після кожного повороту надрукуйте `(int)d`.

Має надрукуватись: `1 2 3 0 1`

**Що це означає:** `enum class` не змішується з `int` без явного приведення ([теорія §2](lab-06-named-bundles.md#2-enum-class--це-типізований-набір-цілих)).

---

## 3. Структура копіюється цілком

Звичайний масив не можна присвоїти іншому. А масив усередині структури — копіюється разом із нею.

### Передбачте

```cpp
#include <cstdint>
#include <iostream>
struct Sprite {
    int x, y;
    int trail[3];
};
int main() {
    Sprite a = {1, 2, {7, 8, 9}};
    Sprite b = a;
    b.x = 100;
    b.trail[0] = 0;
    std::cout << a.x << ' ' << a.trail[0] << '\n';
    std::cout << b.x << ' ' << b.trail[0] << '\n';
}
```

**Вгадайте:** два рядки по два числа.

**Зміни одне:** подумайте: що надрукував би перший рядок, якби `b` була не копією, а посиланням `Sprite& b = a;`?

### Напишіть самі

Напишіть `struct Vec { int x, y; };`, функцію `Vec add(Vec a, Vec b)`, яка повертає суму **копією**, і `void scale(Vec* v, int k)`, яка множить **оригінал**.

Для `add({1, 2}, {3, 4})`, а потім `scale` результату на 2, має надрукуватись:

```txt
4 6
8 12
```

**Що це означає:** присвоєння структури копіює всі поля, разом із масивом ([теорія §1](lab-06-named-bundles.md#1-struct--це-поля-поруч-у-яких-є-імена)).

---

## 4. Вісім кнопок по байту

У Пісочниці 02 вісім кнопок жили в одному байті. А якщо кожній дати свій `bool`?

### Передбачте

```cpp
#include <cstdint>
#include <iostream>
struct Buttons {
    bool up, down, left, right, a, b, start, select;
};
int main() {
    std::cout << sizeof(Buttons) << ' ' << sizeof(std::uint8_t) << '\n';
    Buttons many[100] = {};
    std::cout << sizeof(many) << '\n';
}
```

**Вгадайте:** два числа в першому рядку й одне в другому.

**Зміни одне:** подумайте: скільки байтів займали б ті самі 100 наборів кнопок, якби кожен був одним `std::uint8_t` з бітами?

### Напишіть самі

Напишіть `std::uint8_t pack(const bool b[8])`, яка кладе `b[i]` у біт `i`. Потім розпакуйте байт назад і надрукуйте вісім нулів чи одиниць.

Для `{1, 0, 0, 1, 1, 0, 0, 0}` має надрукуватись:

```txt
25
1 0 0 1 1 0 0 0
```

**Що це означає:** `bool` займає цілий байт, а бітове поле — біт ([теорія §1](lab-06-named-bundles.md#1-struct--це-поля-поруч-у-яких-є-імена)).

---

## 5. Читання після delete

Об'єкт звільнено, а вказівник на нього лишився.

### Передбачте

```cpp
#include <cstdint>
#include <iostream>
struct Point {
    int x, y;
};
int main() {
    Point* p = new Point{3, 4};
    std::cout << p->x + p->y << '\n';
    delete p;
    std::cout << p->x << '\n';
}
```

**Вгадайте:** що надрукує програма і що скаже санітайзер?

**Зміни одне:** одразу після `delete p;` допишіть `p = nullptr;`.

### Напишіть самі

Зробіть правильно. Виділіть на купі масив із трьох `Point` через `new[]`, заповніть `x` числами 1, 2, 3, надрукуйте суму `x`, звільніть через `delete[]` і присвойте вказівнику `nullptr`.

Має надрукуватись `6`, а санітайзер мовчить

**Що це означає:** завислий вказівник — це вказівник, у якого не лишилось об'єкта ([теорія §3](lab-06-named-bundles.md#3-три-часи-життя)).

---

## 6. Два байти, одне число

16-бітне число — це два байти. Покладіть їх у структуру з двох полів і подивіться, котрий куди ліг.

### Передбачте

```cpp
#include <cstdint>
#include <cstring>
#include <iostream>
struct Pair {
    std::uint8_t lo;
    std::uint8_t hi;
};
int main() {
    std::uint16_t value = 0x1234;
    Pair p;
    std::memcpy(&p, &value, sizeof(p));
    std::cout << std::hex << (int)p.lo << ' ' << (int)p.hi << '\n';
}
```

**Вгадайте:** два шістнадцяткові числа.

**Зміни одне:** візьміть `value = 300`.

### Напишіть самі

Тепер назад: напишіть `std::uint16_t join(Pair p)`, яка складає число з двох байтів структури.

Для `join({0x34, 0x12})` і `join({0x2C, 0x01})` має надрукуватись: `4660 300`

**Що це означає:** поля структури лежать у пам'яті по порядку, а число — молодшим байтом уперед ([теорія §1](lab-06-named-bundles.md#1-struct--це-поля-поруч-у-яких-є-імена)).

---

## Здача

У репозиторії:

- файл `sandbox-06.md`, де для кожної задачі є таблиця «мій прогноз · справжній
  вивід · чому так (одне речення)»;
- тека `sandbox-06/` з вашим кодом до «Напишіть самі»: `task1.cpp`, `task2.cpp` і так далі;
- тег `sandbox-06`.

Рівні ті самі, що й в основній лабі, і важать так само:

- **Basic — зараховано:** чотири задачі з шести, обидві половини кожної.
- **Standard — цільовий:** усі шість задач, обидві половини.
- **Advanced — відмінно:** усі шість задач разом із «Зміни одне» в кожній.

**На захисті** викладач бере одну з ваших задач і просить дві речі: передбачити
вивід готового коду після того, як він змінить у ньому одне число, і пояснити або
змінити ваш власний код до «Напишіть самі». Потім одне питання з
[розділу «На захисті» лаби](lab-06-named-bundles.md#на-захисті--поясніть-біля-дошки).

---

<details>
<summary>Для викладача: відповіді (перевірено запуском на clang і gcc)</summary>

| Задача | «Передбачте» | «Зміни одне» |
|---|---|---|
| 1 | `12 8` | з двома `char` — однаково `8`: вони лягли в дірку; з `double` — `16` |
| 2 | `11` | спершу `Green` не знайдеться: тепер це `Color::Green`. Виправили — не збереться `c + 10`. Лише `(int)c + 10` дає знову `11` |
| 3 | `1 7`, `100 0` | `100 0`: посилання — це та сама структура |
| 4 | `8 1`, `800` | `100`: у вісім разів менше |
| 5 | `7`, потім звіт ASan `heap-use-after-free` | звіт інший: UBSan `member access within null pointer`. `nullptr` не лікує баг, але робить його однозначним |
| 6 | `34 12` | `2c 1`: 300 = `0x012C` |

Очікуваний вивід «Напишіть самі» студент бачить у задачі. Розв'язки до кожної з них
написано й запущено, вивід саме такий. На захисті найкраще працює прохання змінити
одне число у власному коді студента й сказати, що тепер надрукується.

</details>
