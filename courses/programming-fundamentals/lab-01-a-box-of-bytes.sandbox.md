# Пісочниця 01 — Коробка байтів: сім маленьких експериментів

**Тижні:** 1–2 · **Лаба:** [Lab 01](lab-01-a-box-of-bytes.md) · **Notes:** [досліди](lab-01-a-box-of-bytes.notes.md) · **Курс:** [EN](README.md) · [UK](README.uk.md)

> **Що це.** Пісочниця — одна з двох стежок курсу. На основній ви лабу за лабою
> будуєте `ember`. Тут проєкту немає: замість нього сім незалежних задач по 20–40
> хвилин, і кожна розкриває одну ідею з теорії Lab 1. Задачі між собою не пов'язані:
> не вийшла одна — беріть наступну.
>
> Стежку обирають один раз, на початку семестру: машина росте з лаби в лабу, і
> приєднатися до неї посеред семестру не вийде.
>
> Теорія та сама, що й у [Lab 01](lab-01-a-box-of-bytes.md): прочитайте розділ
> «Теорія» там. Тут лише задачі.

---

## Як працювати

1. Спершу пройдіть чотири короткі досліди з [Notes 01](lab-01-a-box-of-bytes.notes.md).
   Задачі нижче їх не повторюють, а йдуть далі.
2. Для кожної задачі скопіюйте код у `scratch.cpp` і збирайте тим самим рядком, що
   в notes:

   ```bash
   c++ -std=c++17 -Wall -Wextra -Werror -fsanitize=address,undefined scratch.cpp -o scratch && ./scratch
   ```

3. **Перш ніж запускати**, запишіть у файл `sandbox-01.md` свого репозиторію, що, на
   вашу думку, надрукується. Потім запустіть, допишіть справжній вивід і одне речення:
   чому саме так. Помилитись у прогнозі — нормально. Для цього задачі й існують.

У кожній задачі є «Зміни одне»: маленька зміна в коді, для якої теж спершу
вгадуєте, а потім перевіряєте.

---

## 1. Лічильник, що обнуляється

GPS-супутники передають номер тижня в десяти бітах, тобто від 0 до 1023. У серпні
1999 і у квітні 2019 року лічильник дійшов до кінця й почав з нуля, і приймачі,
які цього не чекали, показали дату двадцятирічної давнини.

```cpp
#include <cstdint>
#include <iostream>
int main() {
    std::uint8_t trip = 0;                    // лічильник на один байт
    for (int km = 0; km < 300; ++km) {
        trip = (std::uint8_t)(trip + 1);
    }
    std::cout << (int)trip << '\n';

    int week = 1023;                          // номер тижня GPS: 0..1023
    week = (week + 1) % 1024;
    std::cout << week << '\n';
}
```

**Вгадайте:** що надрукує кожен рядок?

**Зміни одне:** почніть з `trip = 0` і відніміть одиницю один раз. Що буде? І скільки
ще кілометрів треба проїхати від 44, щоб знову побачити 44?

**Що це означає:** беззнакове число загортається за модулем 2ⁿ, і мова це обіцяє
([теорія §3](lab-01-a-box-of-bytes.md#3-переповнення-рухома-кома-й-інша-брехня)).
Таймери в мікроконтролерах загортаються так постійно — [у залізі](HARDWARE.md#lab-1--коробка-байтів).

---

## 2. Сто годин по одній десятій

У 1991 році система протиракетної оборони Patriot рахувала час десятими частками
секунди. Після ста годин роботи похибка виросла настільки, що система не змогла
супроводити ціль.

```cpp
#include <iostream>
int main() {
    float seconds = 0.0f;
    for (int tick = 0; tick < 3600000; ++tick) {   // 100 годин по 0.1 секунди
        seconds = seconds + 0.1f;
    }
    std::cout << seconds << '\n';

    int tenths = 0;
    for (int tick = 0; tick < 3600000; ++tick) {
        tenths = tenths + 1;
    }
    std::cout << tenths / 10.0 << '\n';
}
```

**Вгадайте:** чи будуть обидва рядки 360000? Якщо ні, який із них більший?

**Зміни одне:** замініть `float` на `double`, а `0.0f` і `0.1f` — на `0.0` і `0.1`.
Тепер обидва рядки однакові? Потім додайте `#include <iomanip>` і друкуйте
`std::cout << std::setprecision(15) << seconds`. Що видно тепер?

**Що це означає:** `0.1` у двійковому записі неточне, і похибка накопичується з кожним
додаванням ([теорія §3](lab-01-a-box-of-bytes.md#3-переповнення-рухома-кома-й-інша-брехня)).
`double` помиляється менше, але теж помиляється. Цілі десяті частки — точно.

---

## 3. HAL і IBM

Комп'ютер у фільмі «Космічна одіссея 2001 року» звуть HAL. Зсуньте кожну літеру на
одну вперед і подивіться, що вийде.

```cpp
#include <iostream>
int main() {
    char word[] = "HAL";
    for (int i = 0; word[i] != '\0'; ++i) {
        word[i] = (char)(word[i] + 1);
    }
    std::cout << word << '\n';
    std::cout << 'a' - 'A' << '\n';
}
```

**Вгадайте:** слово в першому рядку й число в другому.

**Зміни одне:** візьміть `char word[] = "hello world";` і замість `+ 1` відніміть 32.
Що надрукується і чому не все речення?

**Що це означає:** символ — це число, і з ним можна рахувати
([теорія §4](lab-01-a-box-of-bytes.md#4-символ--це-число-в-костюмі)). А рядок
закінчується там, де трапився нульовий байт, навіть якщо ви цього не планували.

---

## 4. Формула, яка завжди дає нуль

Перевести градуси Фаренгейта в Цельсія — шкільна формула: `(f − 32) · 5 / 9`.

```cpp
#include <iostream>
int main() {
    int f = 100;
    int c1 = (f - 32) * 5 / 9;
    int c2 = 5 / 9 * (f - 32);
    double c3 = (f - 32) * 5.0 / 9;
    std::cout << c1 << ' ' << c2 << ' ' << c3 << '\n';
}
```

**Вгадайте:** три числа. Математично `c1` і `c2` — те саме.

**Зміни одне:** `f = 0`. Що тепер покаже `c1`: −18 чи −17?

**Що це означає:** цілочисельне ділення відкидає дріб, і порядок дій вирішує, коли
саме це станеться ([теорія §2](lab-01-a-box-of-bytes.md#2-тип--це-розмір-і-набір-операцій)).
Відкидає воно в бік нуля, а не вниз.

---

## 5. Швидкість, що не влізла

У 1996 році ракета Ariane 5 зруйнувалась на 37-й секунді польоту: швидкість, яку
рахували в 64 бітах, записали в 16-бітне знакове ціле, а вона туди не влізла.

```cpp
#include <cstdint>
#include <iostream>
int main() {
    int speeds[] = {30000, 32767, 32768, 40000, 65536};
    for (int i = 0; i < 5; ++i) {
        std::int16_t stored = (std::int16_t)speeds[i];
        std::cout << speeds[i] << " -> " << stored << '\n';
    }
}
```

**Вгадайте:** що стоятиме праворуч від стрілки в кожному з п'яти рядків?

**Зміни одне:** замініть обидва `std::int16_t` на `std::uint16_t`. Які рядки
змінились?

**Що це означає:** тип — це розмір. У 16 знакових бітах живуть числа від −32768 до
32767, а все більше читається з тих самих бітів як інше число
([теорія §2](lab-01-a-box-of-bytes.md#2-тип--це-розмір-і-набір-операцій)). Чому саме
таке — розповість Lab 2.

---

## 6. Хто спіймає помилку

У кожному фрагменті є помилка. Хто її спіймає: **(А)** компілятор відмовиться
перекладати, **(Б)** компілятор попередить, а `-Werror` зробить з попередження
помилку, **(В)** санітайзер під час виконання чи **(Г)** ніхто?

Кожен фрагмент вставте всередину `int main() { … }` з `#include <iostream>`.

| # | Фрагмент |
|---|---|
| 1 | `int x = 5` (без крапки з комою), далі `std::cout << x << '\n';` |
| 2 | `int x;` далі `std::cout << x << '\n';` |
| 3 | `int big = 2147483647;` `big = big + 1;` `std::cout << big << '\n';` |
| 4 | `int a[3] = {1, 2, 3};` `int i = 3;` `std::cout << a[i] << '\n';` |
| 5 | `unsigned char c = 300;` `std::cout << (int)c << '\n';` |
| 6 | `double d = 0.1 + 0.2;` і `if (d == 0.3)` друкує `equal`, інакше `not equal` |

**Вгадайте:** літеру для кожного рядка. Потім зберіть кожен фрагмент і перевірте.

**Зміни одне:** у фрагменті 4 замініть `a[i]` на `a[3]`. Хто спіймає тепер?

**Що це означає:** помилка може спіймати себе на трьох рівнях — або не спіймати
зовсім ([теорія §1](lab-01-a-box-of-bytes.md#1-виконується-не-той-текст-який-ви-написали)).
Що означають самі повідомлення — в [errors.notes.md](errors.notes.md).

---

## 7. Скільки байтів у слові «Hi»

```cpp
#include <cstring>
#include <iostream>
int main() {
    char s[] = "Hi";
    std::cout << sizeof(s) << ' ' << std::strlen(s) << '\n';
}
```

**Вгадайте:** два числа. У слові дві літери.

**Зміни одне:** `char s[10] = "Hi";`. Які числа тепер?

**Що це означає:** рядок зберігає ще один байт, нульовий, щоб знати, де закінчитись
([теорія §4](lab-01-a-box-of-bytes.md#4-символ--це-число-в-костюмі)). Повністю
рядки чекають у Lab 5.

---

## Здача

У репозиторії — файл `sandbox-01.md`, у ньому для кожної зробленої задачі:

| Задача | Мій прогноз | Справжній вивід | Чому так (одне речення) |
|---|---|---|---|

І тег `sandbox-01`.

Рівні ті самі, що й в основній лабі, і важать так само:

- **Basic — зараховано:** п'ять задач із семи, з прогнозом, виводом і поясненням.
- **Standard — цільовий:** усі сім задач разом із «Зміни одне».
- **Advanced — відмінно:** усе вище плюс **власна восьма задача** в тому самому
  форматі: коротка історія, код, «вгадайте», «зміни одне» і «що це означає». Беріть
  ідею з теорії Lab 1, якої не торкнулась жодна з семи, — скажімо, `const`,
  неініціалізовану змінну чи те, як друкується `char`. На захисті її пояснюєте ви.

**На захисті** викладач бере одну з ваших задач, змінює в ній одне число й просить
передбачити вивід **до** запуску. Потім одне питання з
[розділу «На захисті» лаби](lab-01-a-box-of-bytes.md#на-захисті--поясніть-біля-дошки).
Готуйтесь так само: змініть число у своїй задачі, вгадайте, перевірте.

---

<details>
<summary>Для викладача: відповіді (перевірено запуском; фрагменти задачі 6 — на clang і gcc)</summary>

| Задача | Відповідь | «Зміни одне» |
|---|---|---|
| 1 | `44`, `0` | `255`; ще 256 км |
| 2 | `347025` і `360000`: `float` відстав на 12 975 секунд, тобто на 3,6 години | з `double` обидва рядки `360000`, але з 15 знаками видно `359999.999987987` |
| 3 | `IBM`, `32` | `HELLO`: пробіл (32) мінус 32 — це `'\0'`, і рядок закінчився |
| 4 | `37 0 37.7778`: `5 / 9` — це 0 | `-17 0 -17.7778` |
| 5 | `30000`, `32767`, `-32768`, `-25536`, `0` | з `std::uint16_t` змінюються два рядки: `32768` і `40000` тепер без змін, `65536` — однаково `0` |
| 6 | 1 — А (`expected ';'`); 2 — Б (змінна не ініціалізована); 3 — В (UBSan: знакове переповнення); 4 — В (UBSan: `index 3 out of bounds`); 5 — Б, і компілятор сам пише «300 → 44»; 6 — Г, програма друкує `not equal` | з `a[3]` clang ловить помилку вже під час збірки (Б, `-Warray-bounds`), а gcc без оптимізації — ні, і її ловить санітайзер (В). Різні компілятори бачать різне |
| 7 | `3 2` | `10 2` |

Задачі 1, 3, 4 і 7 — найкращі для «змініть одне число» на захисті: відповідь
рахується в голові за хвилину.

</details>
