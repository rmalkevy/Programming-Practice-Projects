# Пісочниця 07 — Виклик і повернення: шість маленьких задач

**Тижні:** 13–14 · **Лаба:** [Lab 07](lab-07-call-and-return.md) · **Notes:** [досліди](lab-07-call-and-return.notes.md) · **Курс:** [EN](README.md) · [UK](README.uk.md)

> **Що це.** Пісочниця — одна з двох стежок курсу, простіший варіант. На основній ви
> лабу за лабою будуєте `ember`. Тут проєкту немає: замість нього шість незалежних
> задач по 30–50 хвилин, і кожна розкриває одну ідею з теорії Lab 7. Задачі між
> собою не пов'язані: не вийшла одна — беріть наступну.
>
> Теорія та сама, що й у [Lab 07](lab-07-call-and-return.md): прочитайте розділ «Теорія»
> там. Кожна задача має дві половини — **Передбачте** і **Напишіть самі**; як із ними
> працювати, описано в [Пісочниці 01](lab-01-a-box-of-bytes.sandbox.md#як-працювати).
> Прогнози пишіть у `sandbox-07.md`, власний код — у теку `sandbox-07/`.

---

## 1. Факторіал, що не вліз

13! — це понад шість мільярдів. А скільки влазить в `int`?

### Передбачте

```cpp
#include <cstdint>
#include <iostream>
int fact(int n) {
    if (n <= 1) {
        return 1;
    }
    return n * fact(n - 1);
}
int main() {
    for (int n = 10; n <= 13; ++n) {
        std::cout << n << "! = " << fact(n) << '\n';
    }
}
```

**Вгадайте:** чотири рядки. На якому з них щось піде не так?

**Зміни одне:** замініть тип повернення `int fact` на `long long fact`.

### Напишіть самі

Напишіть програму, яка знаходить найбільше n, для якого n! ще влазить в `int`. Рахуйте в `long long` і порівнюйте з 2147483647.

Має надрукуватись: `12`

**Що це означає:** рекурсія працює, але результат має тип, і тип має розмір ([теорія §3](lab-07-call-and-return.md#3-рекурсії-потрібні-базовий-випадок-і-менший-випадок)).

---

## 2. Скільки разів рахує Фібоначчі

Наївний рекурсивний Фібоначчі рахує те саме знову і знову.

### Передбачте

```cpp
#include <cstdint>
#include <iostream>
int calls = 0;
int fib(int n) {
    calls = calls + 1;
    if (n < 2) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}
int main() {
    std::cout << fib(10) << ' ' << calls << '\n';
}
```

**Вгадайте:** `fib(10)` і скільки разів викликалась функція.

**Зміни одне:** порахуйте `fib(20)`.

### Напишіть самі

Напишіть `int fib(int n)` **циклом**, без рекурсії: дві змінні, які крокують уперед.

Для `fib(10)` і `fib(40)` має надрукуватись: `55 102334155` — і миттєво

**Що це означає:** кожен виклик робить ще два, і дерево викликів росте дуже швидко ([теорія §3](lab-07-call-and-return.md#3-рекурсії-потрібні-базовий-випадок-і-менший-випадок)).

---

## 3. Три способи передати

Копія, вказівник і посилання — що з них змінить змінну того, хто викликав?

### Передбачте

```cpp
#include <cstdint>
#include <iostream>
void by_value(int x) { x = x + 1; }
void by_pointer(int* x) { *x = *x + 1; }
void by_reference(int& x) { x = x + 1; }
int main() {
    int a = 10;
    by_value(a);
    std::cout << a << '\n';
    by_pointer(&a);
    std::cout << a << '\n';
    by_reference(a);
    std::cout << a << '\n';
}
```

**Вгадайте:** три числа.

**Зміни одне:** подумайте: що буде, якщо в `main` викликати `by_value(a)` ще десять разів?

### Напишіть самі

Функція може повернути лише одне значення, а посилань — скільки завгодно. Напишіть `void min_max(const int* a, int n, int& mn, int& mx)`.

Для `{4, -2, 9, 0}` має надрукуватись: `-2 9`

**Що це означає:** копія живе у своєму кадрі, а вказівник і посилання ведуть до оригіналу ([теорія §2](lab-07-call-and-return.md#2-як-значення-потрапляють-усередину-й-назовні)).

---

## 4. Рекурсія без кінця

Базовий випадок тут є, але він ніколи не спрацює.

### Передбачте

```cpp
#include <cstdint>
#include <iostream>
int depth(int n) {
    if (n < 0) {
        return 0;
    }
    return 1 + depth(n + 1);
}
int main() {
    std::cout << depth(0) << '\n';
}
```

**Вгадайте:** що надрукує програма?

**Зміни одне:** умову замініть на `n > 1000`.

### Напишіть самі

Напишіть рекурсивну `int digit_sum(int n)` — суму цифр числа — з базовим випадком, який таки спрацює.

Для `digit_sum(12345)` і `digit_sum(0)` має надрукуватись: `15 0`

**Що це означає:** без досяжного базового випадку стек переповнюється ([теорія §3](lab-07-call-and-return.md#3-рекурсії-потрібні-базовий-випадок-і-менший-випадок)).

---

## 5. Вниз і вгору

Кожен виклик має свій кадр, і друк після рекурсивного виклику чекає, поки той повернеться.

### Передбачте

```cpp
#include <cstdint>
#include <iostream>
void count(int n) {
    if (n == 0) {
        return;
    }
    std::cout << "вниз " << n << '\n';
    count(n - 1);
    std::cout << "вгору " << n << '\n';
}
int main() {
    count(3);
}
```

**Вгадайте:** шість рядків по порядку.

**Зміни одне:** поміняйте місцями рядок `std::cout << "вниз "…` і рядок `count(n - 1);`.

### Напишіть самі

Скористайтесь тим, що друк після виклику йде у зворотному порядку. Напишіть рекурсивну `void print_binary(int n)`, яка друкує число в двійковому записі від старшого біта до молодшого.

Для 13, 1 і 8 має надрукуватись:

```txt
1101
1
1000
```

**Що це означає:** у кожного виклику свій кадр і своє `n`, і вони повертаються в зворотному порядку ([теорія §1](lab-07-call-and-return.md#1-виклик--це-стрибок-який-памятає)).

---

## 6. Ханойські вежі

За легендою, ченці переносять 64 золоті диски, і коли закінчать — настане кінець світу.

### Передбачте

```cpp
#include <cstdint>
#include <iostream>
int moves = 0;
void hanoi(int n, char from, char to, char via) {
    if (n == 0) {
        return;
    }
    hanoi(n - 1, from, via, to);
    moves = moves + 1;
    hanoi(n - 1, via, to, from);
}
int main() {
    hanoi(3, 'A', 'C', 'B');
    std::cout << moves << '\n';
}
```

**Вгадайте:** скільки ходів для трьох дисків?

**Зміни одне:** візьміть 10 дисків.

### Напишіть самі

Хай вежі не лише рахують ходи, а й друкують їх. Змініть `hanoi` так, щоб кожен хід друкувався як `A -> C`.

Для двох дисків (з `A` на `C` через `B`) має надрукуватись:

```txt
A -> B
A -> C
B -> C
```

**Що це означає:** задача зводиться до двох менших того самого виду — це і є рекурсія ([теорія §3](lab-07-call-and-return.md#3-рекурсії-потрібні-базовий-випадок-і-менший-випадок)).

---

## Здача

У репозиторії:

- файл `sandbox-07.md`, де для кожної задачі є таблиця «мій прогноз · справжній
  вивід · чому так (одне речення)»;
- тека `sandbox-07/` з вашим кодом до «Напишіть самі»: `task1.cpp`, `task2.cpp` і так далі;
- тег `sandbox-07`.

Рівні ті самі, що й в основній лабі, і важать так само:

- **Basic — зараховано:** чотири задачі з шести, обидві половини кожної.
- **Standard — цільовий:** усі шість задач, обидві половини.
- **Advanced — відмінно:** усі шість задач разом із «Зміни одне» в кожній.

**На захисті** викладач бере одну з ваших задач і просить дві речі: передбачити
вивід готового коду після того, як він змінить у ньому одне число, і пояснити або
змінити ваш власний код до «Напишіть самі». Потім одне питання з
[розділу «На захисті» лаби](lab-07-call-and-return.md#на-захисті--поясніть-біля-дошки).

---

<details>
<summary>Для викладача: відповіді (перевірено запуском на clang і gcc)</summary>

| Задача | «Передбачте» | «Зміни одне» |
|---|---|---|
| 1 | перші три правильні, а на `13!` UBSan каже `signed integer overflow`, і друкується `1932053504` | `13! = 6227020800` |
| 2 | `55 177` | `6765 21891`: число виросло в 123 рази, а викликів — у 124 |
| 3 | `10`, `11`, `12` | нічого не зміниться: `a` лишиться `12` |
| 4 | нічого: звіт ASan `stack-overflow` | `1001` |
| 5 | `вниз 3`, `вниз 2`, `вниз 1`, `вгору 1`, `вгору 2`, `вгору 3` | `вниз 1`, `вгору 1`, `вниз 2`, `вгору 2`, `вниз 3`, `вгору 3` |
| 6 | `7` | `1023`: для n дисків — 2ⁿ − 1 |

Очікуваний вивід «Напишіть самі» студент бачить у задачі. Розв'язки до кожної з них
написано й запущено, вивід саме такий. На захисті найкраще працює прохання змінити
одне число у власному коді студента й сказати, що тепер надрукується.

</details>
